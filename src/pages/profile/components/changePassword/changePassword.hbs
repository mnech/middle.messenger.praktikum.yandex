<form class="{{styles.form}}">
  <div class="{{styles.input}}">
    {{{oldPassword}}}
    <p class="{{styles.error}}">{{errorOldPassword}}</p>
  </div>
  <div class="{{styles.input}}">
    {{{newPassword}}}
    <p class="{{styles.error}}">{{errorPassword}}</p>
  </div>
  <div class="{{styles.btns}}">
    {{{save}}}
    {{{close}}}
  </div>
</form>
